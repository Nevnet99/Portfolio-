import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Navigation from "./Navigation";
import ScrollDecorator from "@stories/ScrollDecorator/ScrollDecorator";

<Meta
  title="Organisms / Navigation"
  component={Navigation}
  decorators={[
    (Story) => (
      <ScrollDecorator>
        <Story />
      </ScrollDecorator>
    ),
  ]}
/>

# Navigation

Navigation component used across the site.

<Canvas>
  <Story name="default" withSource="open">
    <Navigation />
  </Story>
</Canvas>
